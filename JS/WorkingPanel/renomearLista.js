//Função que renomeia uma lista
function renomearLista(nomeAtual, listaID){

    //Pega o texto da lista
    var textoNome = document.getElementById(String("nomeLista" + listaID));

    //Pega o input da lista
    var inputNome = document.getElementById(String("inputNomeLista" + listaID));

}